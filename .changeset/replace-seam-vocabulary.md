---
"mattpocock-skills": patch
---

Replace the word "seam" across the skills and docs with two plainer terms: **test boundary** for the public boundary tests attach to (`tdd`, `to-spec`, `implement`, `diagnosing-bugs`), and **swap point** for the place where an implementation can be swapped behind an interface (`codebase-design` and the skills that borrow its vocabulary). Both carry a Chinese gloss (测试边界, 替换点) so Chinese output no longer falls back to a literal translation.
